[
    {
        "name": "Brute-Force Approach",
        "intuition_and_algorithm": "The extremely naive approach is to use a linear array or list to store the elements of the given matrix and sort the list. We can return the middle element of sorted list to find the median.",
        "images": [],
        "complexities": {
            "time": "O(M*N*(log(M*N))) , sorting the linear array takes time complexity of O(M*N(logM*N)).",
            "space": "O(M*N) , to create a linear array."
        },
        "code": {
            "cpp": "#include <bits/stdc++.h>\nusing namespace std;\n\nclass Solution {\npublic:\n    // Function to find the median of a row-wise sorted matrix\n    int findMedian(vector<vector<int>>& matrix) {\n        \n        // Create a list to store all elements\n        vector<int> elements;\n\n        // Traverse each row of the matrix\n        for (auto& row : matrix) {\n\n            // Traverse each element in the current row\n            for (int val : row) {\n\n                // Add the element to the list\n                elements.push_back(val);\n            }\n        }\n\n        // Sort the collected elements\n        sort(elements.begin(), elements.end());\n\n        // Return the middle element (median)\n        int n = elements.size();\n        return elements[n / 2];\n    }\n};\n\nint main() {\n    Solution obj;\n\n    // Define the matrix\n    vector<vector<int>> matrix = {\n        {1, 3, 5},\n        {2, 6, 9},\n        {3, 6, 9}\n    };\n\n    // Print the median\n    cout << obj.findMedian(matrix) << endl;\n\n    return 0;\n}",
            "java": "import java.util.*;\n\nclass Solution {\n    // Function to find the median of a row-wise sorted matrix\n    public int findMedian(int[][] matrix) {\n\n        // Create a list to store all elements\n        List<Integer> elements = new ArrayList<>();\n\n        // Traverse each row of the matrix\n        for (int[] row : matrix) {\n\n            // Traverse each element in the current row\n            for (int val : row) {\n\n                // Add element to list\n                elements.add(val);\n            }\n        }\n\n        // Sort the list of elements\n        Collections.sort(elements);\n\n        // Return the middle element (median)\n        int n = elements.size();\n        return elements.get(n / 2);\n    }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        Solution obj = new Solution();\n\n        // Define the matrix\n        int[][] matrix = {\n            {1, 3, 5},\n            {2, 6, 9},\n            {3, 6, 9}\n        };\n\n        // Print the median\n        System.out.println(obj.findMedian(matrix));\n    }\n}",
            "python": "class Solution:\n    def findMedian(self, matrix):\n\n        # Create a list to store all elements\n        elements = []\n\n        # Traverse each row\n        for row in matrix:\n\n            # Traverse each value in the row\n            for val in row:\n\n                # Add value to list\n                elements.append(val)\n\n        # Sort all collected elements\n        elements.sort()\n\n        # Return the middle value (median)\n        return elements[len(elements) // 2]\n\n# Driver code\nmatrix = [\n    [1, 3, 5],\n    [2, 6, 9],\n    [3, 6, 9]\n]\n\nobj = Solution()\nprint(obj.findMedian(matrix))",
            "javascript": "class Solution {\n    findMedian(matrix) {\n\n        // Create an array to store all values\n        let elements = [];\n\n        // Traverse every row\n        for (let row of matrix) {\n\n            // Traverse every value in the row\n            for (let val of row) {\n\n                // Add value to the list\n                elements.push(val);\n            }\n        }\n\n        // Sort the array of elements\n        elements.sort((a, b) => a - b);\n\n        // Return the middle element (median)\n        return elements[Math.floor(elements.length / 2)];\n    }\n}\n\n// Driver code\nconst matrix = [\n    [1, 3, 5],\n    [2, 6, 9],\n    [3, 6, 9]\n];\n\nconst obj = new Solution();\nconsole.log(obj.findMedian(matrix));"
        }
    },
    {
        "name": "Optimal Approach",
        "intuition_and_algorithm": "In a row-wise sorted matrix, each row is individually sorted, but the entire matrix isnât globally sorted. Hence, we canât just pick the middle element directly to get the median. If we flatten and sort the entire matrix, it would take O(NÃM log(NÃM)) time, which is inefficient. Instead, we can take advantage of the sorted rows and apply a more optimized method using binary search on the value space (i.e., the range of possible numbers in the matrix).\n    We start by finding the minimum and maximum elements in the matrix. The smallest element will be in the first column, and the largest element will be in the last column. We then binary search between this range to find the median value.\n    In each iteration of the binary search, we choose a middle value and count how many elements in the matrix are less than or equal to it. Since each row is sorted, we can do this efficiently using binary search (upper bound) on each row. If the count is less than or equal to half of the total number of elements, we move our search range to the right, otherwise, we move it to the left.",
        "images": [
            "https://static.takeuforward.org/content/op1.png-4hG4Oy93",
            "https://static.takeuforward.org/content/op2.png-QHabp8GK"
        ],
        "complexities": {
            "time": "O(rows Ã log(max - min) Ã log(cols)) , Binary search runs on the value space from min to max of the matrix and for each mid in binary search, we count how many numbers are less than or equal to mid",
            "space": "O(1) , constant extra space is used."
        },
        "code": {
            "cpp": "#include <bits/stdc++.h>\nusing namespace std;\n\nclass Solution {\npublic:\n    // Function to count elements less than or equal to mid in a row\n    int countLessEqual(vector<int>& row, int mid) {\n        // Using upper_bound to find count efficiently\n        return upper_bound(row.begin(), row.end(), mid) - row.begin();\n    }\n\n    // Function to find median\n    int findMedian(vector<vector<int>>& matrix) {\n        // Number of rows and columns\n        int rows = matrix.size();\n        int cols = matrix[0].size();\n\n        // Minimum possible element in matrix\n        int low = matrix[0][0];\n\n        // Maximum possible element in matrix\n        int high = matrix[0][cols - 1];\n        for (int i = 1; i < rows; i++) {\n            low = min(low, matrix[i][0]);\n            high = max(high, matrix[i][cols - 1]);\n        }\n\n        // Binary search over the value range\n        while (low < high) {\n            int mid = (low + high) / 2;\n\n            // Count elements â¤ mid\n            int count = 0;\n            for (int i = 0; i < rows; i++) {\n                count += countLessEqual(matrix[i], mid);\n            }\n\n            // If count is less than half, median is greater\n            if (count < (rows * cols + 1) / 2)\n                low = mid + 1;\n            else\n                high = mid;\n        }\n\n        // Final low is the median\n        return low;\n    }\n};\n\nint main() {\n    Solution obj;\n    vector<vector<int>> matrix = {\n        {1, 3, 5},\n        {2, 6, 9},\n        {3, 6, 9}\n    };\n    cout << \"Median: \" << obj.findMedian(matrix) << endl;\n    return 0;\n}",
            "java": "import java.util.*;\n\nclass Solution {\n    // Function to count elements less than or equal to mid in a row\n    int countLessEqual(int[] row, int mid) {\n        // Binary search in row\n        int low = 0, high = row.length;\n        while (low < high) {\n            int m = (low + high) / 2;\n            if (row[m] <= mid) low = m + 1;\n            else high = m;\n        }\n        return low;\n    }\n\n    // Function to find the median\n    int findMedian(int[][] matrix) {\n        int rows = matrix.length;\n        int cols = matrix[0].length;\n\n        // Initialize low and high values\n        int low = matrix[0][0];\n        int high = matrix[0][cols - 1];\n        for (int i = 1; i < rows; i++) {\n            low = Math.min(low, matrix[i][0]);\n            high = Math.max(high, matrix[i][cols - 1]);\n        }\n\n        // Binary search in value range\n        while (low < high) {\n            int mid = (low + high) / 2;\n            int count = 0;\n\n            // Count number of elements â¤ mid\n            for (int i = 0; i < rows; i++) {\n                count += countLessEqual(matrix[i], mid);\n            }\n\n            if (count < (rows * cols + 1) / 2)\n                low = mid + 1;\n            else\n                high = mid;\n        }\n\n        // Return the median\n        return low;\n    }\n\n    public static void main(String[] args) {\n        Solution obj = new Solution();\n        int[][] matrix = {\n            {1, 3, 5},\n            {2, 6, 9},\n            {3, 6, 9}\n        };\n        System.out.println(\"Median: \" + obj.findMedian(matrix));\n    }\n}",
            "python": "import bisect\n\nclass Solution:\n    # Function to count elements <= mid using bisect\n    def countLessEqual(self, row, mid):\n        return bisect.bisect_right(row, mid)\n\n    # Function to find median\n    def findMedian(self, matrix):\n        rows = len(matrix)\n        cols = len(matrix[0])\n\n        # Get the smallest and largest elements\n        low = min(row[0] for row in matrix)\n        high = max(row[-1] for row in matrix)\n\n        # Binary search on value space\n        while low < high:\n            mid = (low + high) // 2\n            count = 0\n\n            # Count elements â¤ mid\n            for row in matrix:\n                count += self.countLessEqual(row, mid)\n\n            if count < (rows * cols + 1) // 2:\n                low = mid + 1\n            else:\n                high = mid\n\n        return low\n\n# Driver code\nmatrix = [\n    [1, 3, 5],\n    [2, 6, 9],\n    [3, 6, 9]\n]\nobj = Solution()\nprint(\"Median:\", obj.findMedian(matrix))",
            "javascript": "class Solution {\n    // Function to count number of elements â¤ mid in a row\n    countLessEqual(row, mid) {\n        let low = 0, high = row.length;\n        while (low < high) {\n            let m = Math.floor((low + high) / 2);\n            if (row[m] <= mid) low = m + 1;\n            else high = m;\n        }\n        return low;\n    }\n\n    // Function to find median\n    findMedian(matrix) {\n        let rows = matrix.length;\n        let cols = matrix[0].length;\n\n        // Find global min and max\n        let low = matrix[0][0];\n        let high = matrix[0][cols - 1];\n        for (let i = 1; i < rows; i++) {\n            low = Math.min(low, matrix[i][0]);\n            high = Math.max(high, matrix[i][cols - 1]);\n        }\n\n        // Binary search on value space\n        while (low < high) {\n            let mid = Math.floor((low + high) / 2);\n            let count = 0;\n\n            // Count elements â¤ mid\n            for (let i = 0; i < rows; i++) {\n                count += this.countLessEqual(matrix[i], mid);\n            }\n\n            if (count < Math.floor((rows * cols + 1) / 2))\n                low = mid + 1;\n            else\n                high = mid;\n        }\n\n        return low;\n    }\n}\n\n// Driver code\nconst matrix = [\n    [1, 3, 5],\n    [2, 6, 9],\n    [3, 6, 9]\n];\nconst obj = new Solution();\nconsole.log(\"Median:\", obj.findMedian(matrix));"
        }
    }
]